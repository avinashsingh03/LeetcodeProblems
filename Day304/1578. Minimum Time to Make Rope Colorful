class Solution {
public:
    int minCost(string colors, vector<int>& neededTime) {
        int maximum=0;
        for(int i=0;i<colors.length()-1;i++){
            if(colors[i]==colors[i+1]){
                maximum+=min(neededTime[i],neededTime[i+1]);

                if(neededTime[i]>neededTime[i+1]){
                    neededTime[i+1]=neededTime[i];
                }
            }

        }
        return maximum;
    }
};
//by self
